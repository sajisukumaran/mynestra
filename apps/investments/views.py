"""Investments views — placeholder; fleshed out in the screens step."""

from django.http import HttpResponse


def dashboard(request, *args, **kwargs):
    return HttpResponse("Investments — coming up.")
